<nav class="bg-white border-b border-gray-200 shadow-sm">
	<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
		<div class="flex justify-between items-center h-16">
			<a href="{{ path('app_home') }}" class="flex items-center gap-3">
				<img src="{{ asset('images/logo-black.png') }}" alt="Booker" class="h-8 w-auto" loading="lazy">
			</a>

			<div class="flex items-center gap-4">
				{% if app.user %}
					{% if businesses is not empty %}
						<a href="{{ path('owner_calendar', {id: businesses[0].id}) }}" class="inline-flex items-center gap-2 px-3 py-2 rounded-lg text-gray-700 hover:text-gray-900 hover:bg-gray-100 transition">
							<i class="fa-regular fa-calendar"></i>
							<span>Moje rezerwacje</span>
						</a>
					{% else %}
						<a href="{{ path('user_dashboard') }}" class="inline-flex items-center gap-2 px-3 py-2 rounded-lg text-gray-700 hover:text-gray-900 hover:bg-gray-100 transition">
							<i class="fa-regular fa-user"></i>
							<span>Moje konto</span>
						</a>
					{% endif %}

					{% if app.user.role.value == 'BUSINESS_OWNER' %}
						<a href="{{ path('owner_dashboard') }}" class="inline-flex items-center gap-2 px-3 py-2 rounded-lg text-gray-700 hover:text-gray-900 hover:bg-gray-100 transition">
							<i class="fa-regular fa-building"></i>
							<span>Panel właściciela</span>
						</a>
					{% endif %}

					<form method="post" action="{{ path('app_logout') }}" class="inline">
						<input type="hidden" name="_token" value="{{ csrf_token('logout') }}">
						<button type="submit" class="inline-flex items-center gap-2 px-3 py-2 rounded-lg text-gray-700 hover:text-gray-900 hover:bg-gray-100 transition">
							<i class="fa-solid fa-right-from-bracket"></i>
							<span>Wyloguj</span>
						</button>
					</form>
				{% else %}
					<a href="{{ path('app_login') }}" class="inline-flex items-center gap-2 px-3 py-2 rounded-lg text-gray-700 hover:text-gray-900 hover:bg-gray-100 transition">
						<i class="fa-regular fa-user"></i>
						<span>Zaloguj</span>
					</a>
				{% endif %}
			</div>
		</div>
	</div>
</nav>