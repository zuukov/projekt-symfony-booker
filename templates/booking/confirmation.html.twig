{% extends 'base.html.twig' %}

{% block title %}Potwierdzenie rezerwacji{% endblock %}

{% block body %}
<div class="max-w-7xl mx-auto px-4 mt-8">
    <div class="flex justify-center">
        <div class="w-full max-w-3xl">
            <div class="p-6 mb-6 text-center text-green-800 bg-green-100 border border-green-200 rounded-lg">
                <h2 class="text-2xl font-bold mb-2">
                    <i class="fas fa-check-circle"></i> Rezerwacja została utworzona!
                </h2>
                <p class="text-lg">Twoja rezerwacja została pomyślnie utworzona.</p>
            </div>

            <div class="bg-white rounded-lg shadow">
                <div class="border-b border-gray-200 px-6 py-4">
                    <h4 class="text-xl font-semibold text-gray-900">Szczegóły rezerwacji</h4>
                </div>
                <div class="p-6">
                    <div class="grid grid-cols-1 gap-4">
                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Firma:</strong></div>
                            <div class="sm:w-2/3 text-gray-900">{{ booking.business.businessName }}</div>
                        </div>

                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Usługa:</strong></div>
                            <div class="sm:w-2/3 text-gray-900">{{ booking.service.name }}</div>
                        </div>

                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Pracownik:</strong></div>
                            <div class="sm:w-2/3 text-gray-900">{{ booking.staff.name }} {{ booking.staff.surname }}</div>
                        </div>

                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Data:</strong></div>
                            <div class="sm:w-2/3 text-gray-900">{{ booking.startsAt|date('d.m.Y') }}</div>
                        </div>

                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Godzina:</strong></div>
                            <div class="sm:w-2/3 text-gray-900">{{ booking.startsAt|date('H:i') }} - {{ booking.endsAt|date('H:i') }}</div>
                        </div>

                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Cena:</strong></div>
                            <div class="sm:w-2/3 text-gray-900">{{ booking.priceAtBooking }} PLN</div>
                        </div>

                        <div class="flex flex-col sm:flex-row sm:gap-4">
                            <div class="sm:w-1/3"><strong class="text-gray-700">Status:</strong></div>
                            <div class="sm:w-2/3">
                                <span class="px-2 py-1 text-xs font-semibold rounded-full bg-yellow-100 text-yellow-800">Oczekująca</span>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="border-t border-gray-200 px-6 py-4 text-center">
                    <a href="{{ path('user_bookings') }}" class="px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 inline-block mr-2">Moje rezerwacje</a>
                    <a href="{{ path('app_home') }}" class="px-4 py-2 bg-white border border-gray-300 text-gray-700 rounded-md hover:bg-gray-50 inline-block">Strona główna</a>
                </div>
            </div>

            <div class="p-4 mt-4 text-blue-800 bg-blue-100 border border-blue-200 rounded-lg">
                <strong>Informacja:</strong> Otrzymasz potwierdzenie rezerwacji na swój adres e-mail. Możesz zarządzać swoimi rezerwacjami w zakładce "Moje rezerwacje".
            </div>
        </div>
    </div>
</div>
{% endblock %}
